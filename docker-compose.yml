services:
  tracker:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/tracker.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
